using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CraftingLimit : MonoBehaviour
{
    /*public int limit = 10;
    public int currentCrafted = 0;

    public void CheckLimit(int crafted)
    {
        currentCrafted += crafted;
        if (currentCrafted >= limit)
        {
            Debug.Log("You have reached the crafting limit for today/week.");
            currentCrafted = limit;
        }
    }*/
}
