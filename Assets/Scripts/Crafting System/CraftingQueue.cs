using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CraftingQueue : MonoBehaviour
{
    /*public Queue<CraftingRecipe> queue = new Queue<CraftingRecipe>();

    public void Enqueue(CraftingRecipe recipe)
    {
        queue.Enqueue(recipe);
    }

    public CraftingRecipe Dequeue()
    {
        return queue.Dequeue();
    }

    private void Update()
    {
        if (queue.Count > 0)
        {
            Craft(craftingQueue.Dequeue());
        }
    }*/
}
